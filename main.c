#include <stdio.h>

int main()
{
    int a, b, c, sum, product;
    printf("enter two nos");
    scanf("%d%d", &a, &b);
    sum = a+b+c;
    product = a*b*c;
    printf("output %d", sum, product);

    return 0;
}
